spring:
  application:
    name: test1
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: dev
        group: ${spring.application.name}_GROUP
        file-extension: yaml
